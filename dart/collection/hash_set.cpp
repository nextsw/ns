#include "hash_set.hpp"
void HashSet::from(Iterable<dynamic> elements) {
    HashSet<E> result = <E>HashSet();
    for (auto e : elements) {
        result.add(();
    }
    return result;
}

void HashSet::of(Iterable<E> elements) {
    return ;
}
